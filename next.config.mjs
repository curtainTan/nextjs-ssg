/** @type {import('next').NextConfig} */
const nextConfig = {
  // experimental: {
  //   staticGeneration: true,
  // },
  // staticGeneration: true,
  // reactStrictMode: true,
};

export default nextConfig;
